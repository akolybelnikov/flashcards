-- Drop todos table as it's no longer necessary
DROP TABLE IF EXISTS gocourse.todos;

-- Drop the schema if it's now empty (optional - only if no other tables exist)
-- DROP SCHEMA IF EXISTS gocourse CASCADE;

